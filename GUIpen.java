import javax.swing.*;
import java.awt.*;
import javax.swing.JComponent;

/**
 * Created by SPARK on 2016-02-23.
 */
/*
public class GUIpen extends JComponent {

    private Image image2;

    public static Graphics2D g3;
    // DrawArea.strokeThickness --> represents the thickness of the circle


    protected void paintComponent (Graphics g) {
        if (image2 == null) {
            // image to draw null ==> we create
            image2 = createImage(getSize().width, getSize().height);
            g3 = (Graphics2D) image2.getGraphics();
            g3.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);

        }

        g.drawImage(image2, 0, 0, null);
    }


    DrawArea.g2.setPaint(Color.red);

    g3.drawImage(image2, 0, 0, null);
    g3.fillRect(0,0,getSize().width, getSize().height);
    g3.setPaint(Color.red);


}
*/